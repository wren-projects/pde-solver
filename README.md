# Partial Differential Equation Solver

Package for solving partial differential equations using various methods.

# Developing

We highly suggest you first run the make file (run `make` in the root of the project) as it will load pre-commit hooks.
